# 交易智能体全天候循环 · 全链路优化审计

> 覆盖：候选选股 → 盘前 → 竞价 → 盘中 → 盘后 → 复盘 → 夜间研究 → 假设/池回流。
> 每环节标注：现状组件 / 已闭环 / 缺口与升级建议 / 优先级（P0 本周，P1 两周，P2 数据后）。

## ① 选股与候选池（前一日夜间 → 次日晨）

| 现状 | 状态 |
|---|---|
| 晚间研究 agent：quantdb 打分 26 初筛 → glm 20 池+板块+观点 → `{date}_agent_picks.json`（明晨 load_pool 优先进池） | ✅ 已跑通 |
| quantmind 原复盘 picks 管线（select_from_reports） | ✅ 兼容并存 |

**缺口与升级**
- [P1] **池质量闭环**：每日统计"池内 20 只次日涨跌/当日选中成交数"，写入 `logs/pool_score/{date}.json` 并在晚间研究任务里回看近 20 日命中率 → 池算法持续自评
- [P1] **消息面进池**：初筛 26 只做新闻情绪标注（stock-news-sentiment 技能按代码批量），剔除"利好出尽"形态，理由质量提升
- [P2] 板块温度历史曲线（板块强度按日留存 → 轮动强弱可查）

## ② 盘前 9:10–9:25

| 现状 | 状态 |
|---|---|
| 9:10 风险预算定档（闸门参数） | ✅ |
| 首轮分析注入：昨日复盘要点/已验证假设/盘面状态/情绪温度/近 7 日回顾 | ✅ |
| 持仓预案（复盘 plan/watch）——但**只落文字，未自动挂哨兵** | ⚠️ 缺口 |

**缺口与升级**
- [P0] **复盘 watch → 分钟哨兵自动同步**：`save_watch_rules` 现仅整点分析写入；复盘 plan/watch JSON 盘后生成后调用同一写入器（agent 整组替换语义）→ "破 120 减 30%" 次日 9:30 起真的由哨兵执行，不再靠口头
- [P1] **9:25 竞价快照轮**：轻量任务记录持仓/候选竞价量价（ths-fuyao auction + 桥快照），定"开盘预案执行等级"（正常/降级/暂停），供 9:30 首轮直接使用

## ③ 盘中 9:30–15:00

| 现状 | 状态 |
|---|---|
| 09:35 调仓 + 整点分析 + 波动触发 + 分钟哨兵（watch 触发卖出） | ✅ |
| 决策→闸门→桥下单→分账记账→拒单延期重放 | ✅ |
| 分歧标注（下轮显式回应）与仲裁记录 | ✅ |

**缺口与升级**
- [P1] **预案结局自动回填（复盘用）**：复盘中注入"昨日预案对照表"——预案代码当日是否触发（用今日 bar 最高/最低 vs 触发价自动判定）、触发后走势如何；让复盘从"回忆式"变"对照式"（今天实现）
- [P1] **竞价后开盘等级**联动 09:30 决策（见②）
- [P2] 盘中临时避险通道：极端情绪档（防守+情绪热/冷极值）暂停新开仓 30 分钟冷却

## ④ 盘后 15:00–15:35

| 现状 | 状态 |
|---|---|
| 成交核对（reconcile）、净值尾采样、收盘 bar 入库（quantdb 晚间） | ✅ |
| 无独立环节 | — |

**缺口与升级**：无重大缺口；[P2] 可在 15:05 做一次"收盘快照"写入研究侧（当日盘口/池收盘表现，供复盘引用）。

## ⑤ 复盘 15:35（自我进化）

| 现状 | 状态 |
|---|---|
| 逐笔归因/行为审计/memory 沉淀/明日预案 JSON/假设候选登记 | ✅ |
| 复盘卡进对话流 | ✅ |
| 次日首轮注入复盘要点 | ✅ |

**缺口与升级**
- [P1] **归因的"结局验证"**：复盘当天判定的"该不该卖/买"，次日由 outcome 回填（见③）→ 每周自动统计归因准确率（归因=决策质量 的那几笔后续是否如预期）——让 agent 知道自己复盘准不准
- [P1] memory 治理：append_memory 增长后自动摘要归档（同主题合并）

## ⑥ 夜间研究 19:30

| 现状 | 状态 |
|---|---|
| quantdb 最新数据打分 + glm 板块/20 池 + 池文件 + 研究卡 | ✅ |
| 假设库周测（周日） | ✅ |

**缺口与升级**
- [P1] 池算法自评闭环（见①）
- [P2] 研究任务也注入"已验证假设"与情绪温度 → 池与假设库联动（如"放量大阴次日"证伪 → 打分权重里大阴日不惩罚）

## ⑦ 横向（跨环节）

- [P0] 预案/watch 同步工具（②⑤共用）
- [P1] 运行日报 17:00（已做，纳入 outcome 统计后可自检各环节 DONE）
- [P2] 全链路"标准日"验收脚本：模拟一天产物齐全性检查（报告/池/复盘/日报均存在且非降级）
